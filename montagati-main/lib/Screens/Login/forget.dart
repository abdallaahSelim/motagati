import 'package:flutter/material.dart';

import 'Widget/ForgetView.dart';

class Forget extends StatelessWidget {
  const Forget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: ForgetView(),
    );
  }
}
